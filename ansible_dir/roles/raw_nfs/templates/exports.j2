# /etc/exports: the access control list for filesystems which may be exported
#   to NFS clients.  See exports(5).
/data/nfs            {% for host in groups['kubernetes'] | difference(groups['nfs_server']) %}{{ host }}/24(rw,sync,no_root_squash,no_subtree_check) {% endfor %}